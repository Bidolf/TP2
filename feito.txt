docker imports
	adicionei os requirements que tinhamos

passar o rpc-server para o tp2
	passei as funções todas
	mudei os hosts para db-xml
	não converti o sql para a tabela que está no template do tp2
	
	
import_files.py
	mudei diretorios pra usr os volumes novos
	mudei insert para usar a tabela do initial_schema
	
visualize_table.py
	mudei nomes pra usr tabela e container novo
	

xmlgeneration
	mudei diretorios
		nao sei se está correto
		
		
		
importer
	adicionei o cvs-reader e o cvs-to-xml-converter
		colei nos ficheiros em utils com algumas alterações necessarias
		valida com a schema na pasta /data dos volumes do docker
	a pasta xmlgeneration não é usada, pode ser apagada 
	a pasta entities não é usada
		era pra separar os elemento em cada linha para as "tabelas" como entidades
		como nos sightings, nenhum elemento é reutilizado 
			(data é quase sempre unica, lugar é quase sempre único)
			não separei em classes
	
	fiz os 3 TODOs da main
		check converted files in the database
		once the conversion is done, we should updated the converted_documents tables
		we should store the XML document into the imported_documents table
		you should retrieve from the database the files that were already converted before
		
		
	csv agora cria vários xml
	
	NÃO CONSEGUI POR O "encoding='utf-8'" NO TOPO DOS FICHEIROS XML
	
	
rpc-server
	corrigi os comandos sql no
		db_addfiles, db_delete
		db_exists
		retrieve xml, retrieve xml group by file
		visualize table
		
	não corrigi o import_filee o drop_table pq acho que não vai ser usado
		